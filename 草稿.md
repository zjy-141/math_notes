## 1. 对称差不等式

**题目**：设 $A,B$ 均为 Jordan 可测集，证明  
$$|m(A)-m(B)|\le m(A\triangle B).$$

**证明**：  
因为
$$A=(A\cap B)\cup(A\setminus B),\qquad B=(A\cap B)\cup(B\setminus A),$$
且各部分不交，由 Jordan 测度的有限可加性，
$$m(A)=m(A\cap B)+m(A\setminus B),\qquad m(B)=m(A\cap B)+m(B\setminus A).$$
于是
$$|m(A)-m(B)|=|m(A\setminus B)-m(B\setminus A)|\le m(A\setminus B)+m(B\setminus A)=m(A\triangle B).$$
证毕。

---

## 2. 内测度与闭集、外测度与开集

**题目**：设 $E\subset\mathbb R^n$ 有界，证明  
$$m_*(E)=\sup_{F\subseteq E,\ F\text{ 为闭集}}m(F),\qquad m^*(E)=\inf_{G\supseteq E,\ G\text{ 为开集}}m(G).$$
由于 $E$ 有界，也可写为“有界闭集”“有界开集”。

**证明**：  
先证内测度。按定义
$$m_*(E)=\sup\{m(S):S\subseteq E,\ S\text{ 为简单集}\}.$$
简单集是闭集，故
$$\sup_{F\subseteq E,\ F\text{ 闭}}m(F)\ge m_*(E).$$
反过来，任取闭集 $F\subseteq E$，则 $F$ 有界闭，因而是紧集，且
$$m(F)=m_*(F)=\sup\{m(S):S\subseteq F,\ S\text{ 为简单集}\}\le \sup\{m(S):S\subseteq E,\ S\text{ 为简单集}\}=m_*(E).$$
所以
$$\sup_{F\subseteq E,\ F\text{ 闭}}m(F)\le m_*(E).$$
故内测度等式成立。

再证外测度。按定义
$$m^*(E)=\inf\left\{\sum |Q_i|:E\subseteq \bigcup_i Q_i,\ Q_i\text{ 为开矩体}\right\}.$$
任取开集 $G\supseteq E$，由单调性 $m(G)\ge m^*(E)$，故
$$\inf_{G\supseteq E,\ G\text{ 开}}m(G)\ge m^*(E).$$
另一方面，任取覆盖 $E\subseteq \bigcup_i Q_i$，令 $G=\bigcup_i Q_i$，则 $G$ 为开集，$G\supseteq E$，且
$$m(G)\le \sum_i |Q_i|.$$
因此
$$\inf_{G\supseteq E,\ G\text{ 开}}m(G)\le \inf \sum_i |Q_i|=m^*(E).$$
故外测度等式成立。证毕。

---

## 3. 内测度 + 外测度 = 简单集测度

**题目**：设 $S\subset\mathbb R^n$ 有界，$E$ 是包含 $S$ 的简单集。证明  
$$m_*(S)+m^*(E\setminus S)=m(E).$$

**证明**：  
因为 $E$ 是简单集，故
$$m(E)=m^*(E)=m_*(E).$$

先证 $m_*(S)+m^*(E\setminus S)\le m(E)$。  
任取 $\varepsilon>0$。由内测度定义，存在简单集 $A\subseteq S$，使得
$$m(A)>m_*(S)-\varepsilon.$$
由于 $A\subseteq S\subseteq E$，所以 $E\setminus A$ 是简单集，且
$$E\setminus A\supseteq E\setminus S.$$
于是
$$m^*(E\setminus S)\le m(E\setminus A)=m(E)-m(A)<m(E)-m_*(S)+\varepsilon.$$
因此
$$m_*(S)+m^*(E\setminus S)\le m(E)+\varepsilon.$$
令 $\varepsilon\to0^+$，得
$$m_*(S)+m^*(E\setminus S)\le m(E).$$

再证反向。任取 $\varepsilon>0$。由外测度定义，存在简单集 $B\supseteq E\setminus S$，使得
$$m(B)<m^*(E\setminus S)+\varepsilon.$$
若 $B$ 超出 $E$，用 $B\cap E$ 代替 $B$，仍为简单集，且
$$B\cap E\supseteq E\setminus S,\qquad m(B\cap E)\le m(B).$$
故可设 $B\subseteq E$。于是
$$E\setminus B\subseteq S,$$
且 $E\setminus B$ 是简单集。由内测度定义，
$$m_*(S)\ge m(E\setminus B)=m(E)-m(B)>m(E)-m^*(E\setminus S)-\varepsilon.$$
所以
$$m_*(S)+m^*(E\setminus S)\ge m(E)-\varepsilon.$$
令 $\varepsilon\to0^+$，得
$$m_*(S)+m^*(E\setminus S)\ge m(E).$$
综上，等式成立。证毕。

---

## 4. Jordan 测度的介值定理

**题目**：设 $A,B$ 均为 Jordan 可测，且 $A\subseteq B$。证明：对任意
$$\alpha\in[m(A),m(B)],$$
存在 Jordan 可测集 $C$，满足
$$A\subseteq C\subseteq B,\qquad m(C)=\alpha.$$

**证明**：  
令
$$E_t=[-t,t]^n,\qquad t>0.$$
由于 $B$ 有界，取 $T>0$ 使 $B\subseteq E_T$。定义
$$f(t)=m\bigl(A\cup(B\cap E_t)\bigr),\qquad t\in[0,T].$$
则当 $t=0$ 时（或取足够小的 $t$ 使 $B\cap E_t=\varnothing$），
$$f(0)=m(A);$$
当 $t=T$ 时，
$$f(T)=m(A\cup B)=m(B).$$
并且 $f$ 连续：对 $t\in[0,T]$，$\delta>0$，
$$|f(t+\delta)-f(t)|\le m\bigl(B\cap(E_{t+\delta}\setminus E_t)\bigr)\le m(E_{t+\delta}\setminus E_t)=(2(t+\delta))^n-(2t)^n\to0$$
当 $\delta\to0^+$。因此 $f$ 在 $[0,T]$ 上连续。由连续函数介值定理，对任意
$$\alpha\in[f(0),f(T)]=[m(A),m(B)],$$
存在 $t_0\in[0,T]$，使得
$$f(t_0)=\alpha.$$
令
$$C=A\cup(B\cap E_{t_0}).$$
则 $C$ 是 Jordan 可测集，且
$$A\subseteq C\subseteq B,\qquad m(C)=f(t_0)=\alpha.$$
证毕。

---

## 5. Jordan 可测集的可列可加性

**题目**：设 $\{E_j\}_{j=1}^\infty$ 是 $\mathbb R^n$ 中两两不交的 Jordan 可测集，且
$$\bigcup_{j=1}^\infty E_j$$
也是 Jordan 可测。证明
$$m\left(\bigcup_{j=1}^\infty E_j\right)=\sum_{j=1}^\infty m(E_j).$$

**证明**：  
记
$$E=\bigcup_{j=1}^\infty E_j.$$
由于各 $E_j$ 两两不交，对任意 $N$，由有限可加性，
$$\sum_{j=1}^N m(E_j)=m\left(\bigcup_{j=1}^N E_j\right)\le m(E).$$
令 $N\to\infty$，得
$$\sum_{j=1}^\infty m(E_j)\le m(E).$$

反向：任取 $\varepsilon>0$。因为 $E$ 是 Jordan 可测，所以存在简单集 $F\subseteq E$，使得
$$m(F)>m(E)-\varepsilon.$$
对每个 $j$，由外测度定义，存在开集 $G_j\supseteq E_j$，使得
$$m(G_j)<m(E_j)+\frac{\varepsilon}{2^j}.$$
于是
$$F\subseteq E\subseteq \bigcup_{j=1}^\infty G_j.$$
由于 $F$ 是简单集，有界闭，从而是紧集，故开覆盖 $\{G_j\}$ 有有限子覆盖，即存在有限个指标 $j_1,\dots,j_k$，使得
$$F\subseteq \bigcup_{i=1}^k G_{j_i}.$$
因此
$$m(F)\le m\left(\bigcup_{i=1}^k G_{j_i}\right)\le \sum_{i=1}^k m(G_{j_i})< \sum_{i=1}^k \left(m(E_{j_i})+\frac{\varepsilon}{2^{j_i}}\right)\le \sum_{j=1}^\infty m(E_j)+\varepsilon.$$
于是
$$m(E)<m(F)+\varepsilon< \sum_{j=1}^\infty m(E_j)+2\varepsilon.$$
令 $\varepsilon\to0^+$，得
$$m(E)\le \sum_{j=1}^\infty m(E_j).$$
结合前面的反向不等式，得到
$$m(E)=\sum_{j=1}^\infty m(E_j).$$
证毕。