
## 一. $\Pi(x)$

把正整数上定义的函数 $n!$ 延拓成实数集上的一个连续函数 $\Pi(x)$，满足：
$$
\Pi(x) = \prod_{k=1}^{\infty} \left(1+\frac{x}{k}\right)^{-1}\left(1+\frac{1}{k}\right)^x \quad (x\in R_{\ge 0})
$$

**证明**：
 $$
 \begin{aligned}
 \prod_{k=1}^{N} \left(1+\frac{x}{k}\right)^{-1}\left(1+\frac{1}{k}\right)^x 
 &= \frac{1\cdot2\cdots N}{(x+1)\cdots(x+N)} \cdot (N+1)^x \\
 &= \frac{1\cdot2\cdots x}{(N+1)\cdots(N+x)} \cdot (N+1)^x \\
 &= \frac{N+1}{N+1}\cdots\frac{N+1}{N+x} \cdot x! \\
 \end{aligned}
 $$

令 $N\rightarrow +\infty$ 得 $\Pi(x)=x!$ 。

下证 $\log\Pi(x)$ 在 $[a,b]$ 上连续即可（$b>a>0$）：
$$
\begin{aligned}
(1+\frac{x}{k})^{-1}(1+\frac{1}{k})^x &= (1-\frac{x}{k}+o(\frac{1}{k}))(1+\frac{x}{k}+o(\frac{1}{k})) \\&= 1+O(\frac{1}{k^2}) \quad (k\rightarrow +\infty)
\end{aligned}
$$
$$
\begin{aligned}
\log(1+\frac{x}{k})^{-1}(1+\frac{1}{k})^x = \log(1+O(\frac{1}{k^2})) = O(\frac{1}{k^2}) \quad (k\rightarrow +\infty)
\end{aligned}
$$

则 $\exists N$ 使得 $\sum_{k=N+1}^{\infty} \log(1+\frac{x}{k})^{-1}(1+\frac{1}{k})^x = \sum_{k=N+1}^{\infty} O(\frac{1}{k^2})<\varepsilon$ 。
故 $\sum_{k=1}^{\infty} \log(1+\frac{x}{k})^{-1}(1+\frac{1}{k})^x$ 在任意 $[a,b]$ 上一致收敛，
由于 $\log(1+\frac{x}{k})^{-1}(1+\frac{1}{k})^x$ 连续，且一致收敛知 $\sum_{k=1}^{\infty} \log(1+\frac{x}{k})^{-1}(1+\frac{1}{k})^x$ 连续，故 $\log\Pi(x)$ 在 $[a,b]$ 上连续。

故 $\Pi(x)$ 在 $[a,b]$ 上连续。

---

## 二. $\Gamma(x)$

记 $\Gamma(x)=\Pi(x-1)=\frac{1}{x}\Pi(x)$，则 $\Gamma(x+1)=x\cdot \Gamma(x)$ 。
$$
\Gamma(x)^{-1} = x \cdot \prod_{k=1}^{\infty} \left(1+\frac{x}{k}\right) \left(1+\frac{1}{k}\right)^{-x} \quad (x\in R_{\ge 0})
$$

对 $\forall s>0$，有：
$$
\Gamma(s) = \int_0^{+\infty} e^{-x} x^{s-1} dx
$$

**证明**：
由定义得：
$$
\Gamma(s) = \frac{1}{s} \prod_{k=1}^{\infty} \left(\frac{k}{k+s}\right) \left(\frac{k+1}{k}\right)^s
$$

由分部积分法进一步展开计算：
$$
\begin{aligned}
&\frac{1}{s} \prod_{k=1}^{N} \left(\frac{k}{k+s}\right) \left(\frac{k+1}{k}\right)^s \\
&= \frac{1\cdot 2\cdots N}{s(s+1)\cdots(s+N)} (N+1)^s \\
&= (N+1)^s \frac{1\cdot 2\cdots N}{s(s+1)\cdots(s+N)} (s+N) \int_0^1 x^{s+N-1} dx \\
&= (N+1)^s \frac{2\cdot 3\cdots N}{s(s+1)\cdots(s+N-1)} \left[ -\int_0^1 x^{s+N-1} d(1-x) \right] \\
&= (N+1)^s \frac{2\cdot 3\cdots N}{s(s+1)\cdots(s+N-1)} \left[ 0 + \int_0^1 (1-x) d x^{s+N-1} \right] \\
&= (N+1)^s \frac{2\cdot 3\cdots N}{s(s+1)\cdots(s+N-2)} \cdot \int_0^1 (1-x) x^{s+N-2} dx \\
&= (N+1)^s \frac{3\cdot 4\cdots N}{s(s+1)\cdots(s+N-3)} \left[-\int_0^1  x^{s+N-3} d(1-x)^2 \right]\\
&=\cdots\\
&= (N+1)^s \frac{k\cdot(k+1)\cdots N}{s(s+1)\cdots(s+N-k)} \cdot \int_0^1 (1-x)^{k-1} \cdot x^{s+N-k} dx \\
&= (N+1)^s \frac{(k+1)\cdot(k+2)\cdots N}{s(s+1)\cdots(s+N-k)} \left[-\int_0^1  x^{s+N-k} d(1-x)^{k} \right]\\
&= (N+1)^s \frac{(k+1)\cdot(k+2)\cdots N}{s(s+1)\cdots(s+N-k-1)} \cdot \int_0^1 (1-x)^{k} \cdot x^{s+N-k-1} dx \\
&=\cdots\\
&= (N+1)^s \cdot \int_0^1 (1-x)^N \cdot x^{s-1} dx \\
&= N^s \int_0^1 (1-x)^N \cdot x^{s-1} dx \cdot \frac{(N+1)^s}{N^s} \\
&= \int_0^1 (1-x)^N \cdot (Nx)^{s-1} d(Nx) \cdot \left(\frac{N+1}{N}\right)^s \\
&= \int_0^N \left(1-\frac{x}{N}\right)^N \cdot x^{s-1} dx \cdot \left(\frac{N+1}{N}\right)^s
\end{aligned}
$$

利用指数放缩可得上下界关系：
$$
e^{-x} = (e^{-\frac{x}{N}})^N \ge (1-\frac{x}{N})^N
$$
以及：
$$
\begin{aligned}
&\quad e^{-x} - (1-\frac{x}{N})^N \\
&\le e^{-x}\left[ 1 - e^x\cdot (1-\frac{x}{N})^N \right]\\
&= e^{-x}\left[ 1 - (e^{\frac{x}{N}})^N\cdot (1-\frac{x}{N})^N \right]\\
&\le e^{-x}\left[ 1 - (1+\frac{x}{N})^N(1-\frac{x}{N})^N \right]\\
&= e^{-x}\left[ 1 - (1-\frac{x^2}{N^2})^N \right]\\
&\le e^{-x}\left[ 1 - (1-\frac{x^2}{N}) \right] \\
&= \frac{x^2}{N\cdot e^x}
\end{aligned}
$$

由此得到积分差值估计：
$$
0 \le \int_0^N e^{-x} x^{s-1} dx - \int_0^N \left(1-\frac{x}{N}\right)^N x^{s-1} dx \le \int_0^N \frac{x^2}{N\cdot e^x}  x^{s-1} dx
$$
即：
$$
0 \le \int_0^N e^{-x} x^{s-1} dx - \int_0^N \left(1-\frac{x}{N}\right)^N x^{s-1} dx \le  \frac{1}{N} \int_0^N \frac{x^{s+1}}{e^x} dx = O(\frac{1}{N})
$$

最终取极限得到伽马函数的积分表达式：
$$
\begin{aligned}
\Gamma(s) 
&= \lim_{N\rightarrow\infty} \frac{1}{N^s} \sum_{k=1}^{N} \left(\frac{k}{k+s}\right)\left(\frac{k+1}{k}\right)^s \\
&= \lim_{N\rightarrow\infty} \int_0^N \left(1-\frac{x}{N}\right)^N x^{s-1} dx \cdot \left(\frac{N+1}{N}\right)^s \\
&= \lim_{N\rightarrow\infty} \int_0^N \left(1-\frac{x}{N}\right)^N x^{s-1} dx \cdot 1\\
&= \lim_{N\rightarrow\infty} \int_0^N e^{-x} \cdot x^{s-1} dx \\
&= \int_0^{+\infty} e^{-x} x^{s-1} dx
\end{aligned}
$$

---

## 三、斯特林公式 ($n!$)

$n! = (\frac{n}{e})^n \sqrt{2\pi n} \cdot (1+O(\frac{1}{n}))$ ，
$n! \sim (\frac{n}{e})^n \sqrt{2\pi n}$ 

**证明**：
由欧拉求和公式得：
$$
\begin{aligned}
\log n! 
&= \sum_{k=1}^n \log k \\
&=\log 1+ \int_1^n \log t dt + \int_1^n \frac{\psi(t)}t dt + \frac{1}{2} \log n \\
&= (t\log t - t)\bigg|_1^n + \int_1^n \frac{\psi(t)}t dt + \frac{1}{2} \log n \\
&= n\log n - n + 1 + \frac{1}{2} \log n + \int_1^{+\infty} \frac{\psi(t)}t dt - \int_n^{+\infty} \frac{\psi(t)}t dt
\end{aligned}
$$
 
 由微分第二中值定理得：
$$
\int_n^{+\infty} \frac{\psi(t)}t dt = \lim_{N\rightarrow\infty}  \int_n^N \frac{\psi(t)}t dt = \lim_{N\rightarrow\infty} \frac{1}{n} \int_n^{\xi} \psi(t) dt = O(\frac{1}{n})
$$

故：
$$
\log n! = n\log n + \frac{1}{2} \log n - n + 1 + \int_1^{+\infty} \frac{1}{2} \psi(t) dt + O(\frac{1}{n})
$$

由于 $\int_1^{+\infty} \psi(t) dt$ 收敛且 $\frac{1}{t}$ 单调趋于 0，则 $\int_1^{+\infty} \frac{\psi(t)}t dt$ 收敛，
记 $\log A = 2 + 2 \int_1^{+\infty} \frac{1}{t} \psi(t) dt$，则 $\log n! = n\log n + \frac{1}{2} \log (An) - n + O(\frac{1}{n})$。

整理得：
$$
n! = \left(\frac{n}{e}\right)^n \sqrt{An} \cdot e^{O(\frac{1}{n})} = \left(\frac{n}{e}\right)^n \sqrt{An} \left[ 1 + O(\frac{1}{n}) \right]
$$
由 Wallis 公式 $\frac{\pi}{2} = \lim_{n\rightarrow\infty}  \frac{[(2n)!!]^2}{(2n-1)!!(2n+1)!!}  = \lim_{n\rightarrow\infty} \frac{2^{4n} (n!)^4}{(2n+1) \cdot ((2n)!)^2}$

代入 $n!$ 得：
$$
\begin{aligned}
\frac{\pi}{2} 
&= \lim_{n\rightarrow\infty} \frac{2^{4n} \left[ \left(\frac{n}{e}\right)^{n} \sqrt {An} \cdot (1+O(\frac{1}{n})) \right]^4}{(2n+1) \cdot \left[\left(\frac{2n}{e}\right)^{2n} \cdot \sqrt {2An} \cdot (1+O(\frac{1}{n}))\right]^2} \\
&= \lim_{n\rightarrow\infty} \frac{2^{4n}  \left(\frac{n}{e}\right)^{4n} (An)^2 \cdot (1+O(\frac{1}{n}))^4}{(2n+1) \cdot \left(\frac{2n}{e}\right)^{4n} \cdot 2An \cdot (1+O(\frac{1}{n}))^2} \\
&= \lim_{n\rightarrow\infty} \frac{An\cdot(1+O(\frac{1}{n}))^2}{(2n+1)\cdot 2} \\
&= \lim_{n\rightarrow\infty} \frac{A}{4}
\end{aligned}
$$
即 $A=2\pi$。

因此：
$$
n! = \left(\frac{n}{e}\right)^n \sqrt{2\pi n} (1+O(\frac{1}{n}))
$$

> 由 $A$ 的值得 $\int_1^{+\infty} \frac{1}{t} \psi(t) dt=\frac{1}{2}\log 2\pi -1$ 。

---

## 四、斯特林公式 ($\Gamma(x)$)

$\Gamma(x) = (\frac{x}{e})^x \sqrt{\frac{2\pi}{x}} (1+O(\frac{1}{x}))$ ，
$\Gamma(x+1) = x\Gamma(x) = (\frac{x}{e})^x \sqrt{2\pi x} (1+O(\frac{1}{x}))$

**证明**：
由定义得：
$$
\Gamma(x) = \frac{1}{x} \prod_{k=1}^{\infty} \left(\frac{k}{k+x}\right) \left(\frac{k+1}{k}\right)^x
$$则： $$
 \begin{aligned}
 \log \Gamma(x) &= \log \frac{1}{x} \prod_{k=1}^{\infty} \frac{k}{k+x} \cdot \left(\frac{k+1}{k}\right)^x\\
 & = -\log x + \sum_{k=1}^{\infty} \log \left(\frac{k}{k+x}\right) + \sum_{k=1}^{\infty} \log \left(\frac{k+1}{k}\right)^x
 \end{aligned}
 $$
对 $\sum_{k=1}^{\infty} \log \left(\frac{k}{k+x}\right)$ 的前 $N$ 项应用欧拉求和公式得：
$$
\begin{aligned}
\sum_{k=1}^{N} \log \frac{k}{k+x} &= \log \frac {1}{x+1} + \int_1^{N} \log \frac{t}{x+t} dt + \int_1^{N}(\frac{1}{t}-\frac{1}{t+x})\psi(t) dt \\
&\quad- \frac{1}{2} \log \frac{1}{x+1} + \frac{1}{2} \log \frac{N}{N+x} \\
&= \frac{1}{2} \log \frac{1}{x+1} + (t\log t - (t+x)\log (x+t))\bigg|_1^N \\
&\quad+ \frac{1}{2} \log \frac{N}{x+N} + \int_1^{N}(\frac{1}{t}-\frac{1}{t+x})\psi(t) dt \\
&= \frac{1}{2} \log \frac{1}{x+1} + N\log N - (N+x)\log (N+x)+(x+1)\log(x+1) \\
&\quad+ \frac{1}{2} \log \frac{N}{x+N} + \int_1^{N}(\frac{1}{t}-\frac{1}{t+x})\psi(t) dt 
\end{aligned}
$$

处理其中的 $\int_1^{N}(\frac{1}{t}-\frac{1}{t+x})\psi(t) dt$ 项：
$$
\begin{aligned}
\int_1^{N}(\frac{1}{t}-\frac{1}{t+x})\psi(t) dt  
&= \int_1^{+\infty} \frac{\psi(t)}{t} dt - \int_N^{+\infty} \frac{\psi(t)}{t} dt -\int_1^N \frac{\psi(t)}{t+x} dt \\
&= \int_1^{+\infty} \frac{\psi(t)}{t} dt - O(\frac{1}{N}) -\frac{1}{x+1}\int_1^{\xi} \psi(t) dt \\
&= \frac{1}{2}\log 2\pi -1- O(\frac{1}{N}) -O(\frac{1}{x})\\
\end{aligned}
$$

代入原式计算并取极限 $N \to \infty$：

$$
\begin{aligned}
&\log\Gamma(x)+\log(x)\\
&=\sum_{k=1}^{\infty} \log \left(\frac{k}{k+x}\right) + \sum_{k=1}^{\infty} \log \left(\frac{k+1}{k}\right)^x\\
&=\lim_{N \rightarrow \infty} \bigg[ \frac{1}{2} \log \frac{1}{x+1} + N\log N - (N+x)\log (N+x)+(x+1)\log(x+1) \\
&\quad+ \frac{1}{2} \log \frac{N}{x+N} + \frac{1}{2}\log 2\pi -1- O(\frac{1}{N}) -O(\frac{1}{x})+ x\log (N+1) \bigg]\\
&=\frac{1}{2} \log \frac{1}{x+1} + (x+1)\log(x+1) +\frac{1}{2}\log 2\pi -1 -O(\frac{1}{x}) + \lim_{N \rightarrow \infty} \bigg[N\log N \\
&\quad- (N+x)\log (N+x) + \frac{1}{2} \log \frac{N}{x+N} - O(\frac{1}{N}) + x\log (N+1) \bigg]\\
&= (x+\frac{1}{2})\log(x+1) +\frac{1}{2}\log 2\pi -1 -O(\frac{1}{x}) + \lim_{N \rightarrow \infty} \bigg[N\log N -N\log (N+x) \\
&\quad- x\log (N+x)+ \frac{1}{2} \log \frac{N}{x+N} - O(\frac{1}{N}) + x\log (N+1) \bigg]\\
&= \cdots + \lim_{N \rightarrow \infty} \bigg[-N\log \frac{N+x}N - x\log \frac{N+x}{N+1}- \frac{1}{2} \log \frac{x+N}{N} - O(\frac{1}{N})\bigg]\\
&= \cdots + \lim_{N \rightarrow \infty} \bigg[-N\log (1+\frac{x}{N}) - x\log(1+\frac{x-1}{N+1}) - \frac{1}{2} \log (1+\frac{x}{N}) - O(\frac{1}{N})\bigg]\\
&= \cdots + \lim_{N \rightarrow \infty} \bigg[-N\Big(\frac{x}{N}+O(\frac{x^2}{N^2})\Big) - x \Big(O(\frac{x-1}{N+1})\Big) - \frac{1}{2} \Big(O(\frac{x}{N})\Big) - O(\frac{1}{N})\bigg]\\
&= \cdots -x + \lim_{N \rightarrow \infty} \bigg[-\Big(O(\frac{x^2}{N})\Big) - \Big(O(\frac{x(x-1)}{N+1})\Big) - \Big(O(\frac{x}{N})\Big) - O(\frac{1}{N})\bigg]\\
&= \cdots -x - \lim_{N \rightarrow \infty} \bigg[O(\frac{x^2}{N})\bigg]\\
&= (x+\frac{1}{2})\log(x+1) +\frac{1}{2}\log 2\pi -1 -O(\frac{1}{x}) -x - \lim_{N \rightarrow \infty} \bigg[O(\frac{x^2}{N})\bigg]\\
\end{aligned}
$$
即

$$
\begin{aligned}
\log\Gamma(x)
&=-\log(x)+(x+\frac{1}{2})\log(x+1)-x +\frac{1}{2}\log 2\pi -1 -O(\frac{1}{x}) \\
&=-\log(x)+(x+\frac{1}{2})\Big(\log(x)+\log(1+\frac1 x)\Big)-x +\frac{1}{2}\log 2\pi -1 -O(\frac{1}{x}) \\
&=-\log(x)+(x+\frac{1}{2})\Big(\log(x)+\frac{1}{x}+O(\frac{1} {x^2})\Big)-x +\frac{1}{2}\log 2\pi -1 -O(\frac{1}{x}) \\
&=-\log(x)+x\log(x)+1+O(\frac1 x)+\frac{1}{2}\log(x)-x +\frac{1}{2}\log 2\pi -1 -O(\frac{1}{x}) \\
&=x\log(x)+x +\frac{1}{2}\log \frac{2\pi}{x} +O(\frac{1}{x}) \\
\end{aligned}
$$

因此，最终得到结果：
$$
\Gamma(x) = \left(\frac{x}{e}\right)^x \sqrt{\frac{2\pi}{x}} (1+O(\frac{1}{x}))
$$

## 五、$B$ 函数
定义 $B$ 函数
$$
B(p,q)=\int_0^1 x^{p-1}(1-x)^{q-1}\;dx
$$
有 
$$
B(p,q)=\frac{\Gamma(p)\Gamma(q)}{\Gamma(p+q)},\quad \forall\, p,q>0
$$
**证明**：
已知：
$$
\Gamma(s) = \int_0^{+\infty} e^{-x} x^{s-1} dx,\quad \forall s>0
$$
### 分类一
先考虑 $p>1,q>1$ 的情况，有：
$$
\begin{aligned}
\Gamma(p)\Gamma(q)
&=\Gamma(q)\int_0^{+\infty}e^{-x}\cdot x^{p-1}dx\\
&=\int_0^{+\infty} e^{-x}\cdot x^{p-1}\Gamma(q) dx\\
&=\int_0^{+\infty} e^{-x}\cdot x^{p-1} \left( \int_0^{+\infty}e^{-y}\cdot y^{q-1}dy \right)dx\\
&=\int_0^{+\infty}  \left( \int_0^{+\infty}  e^{-(x+y)}\cdot x^{p-1}\cdot y^{q-1}dy \right)dx\\
\end{aligned}
$$
令 $y=xz$ 得
$$
\begin{aligned}
\Gamma(p)\Gamma(q)
&=\int_0^{+\infty}  \left( \int_0^{+\infty}  e^{-(x+xz)}\cdot x^{p-1}\cdot(xz)^{q-1} \,d(xz) \right)dx\\
&=\int_0^{+\infty}  \left( \int_0^{+\infty}  e^{-x(1+z)}\cdot x^{p+q-1}\cdot z^{q-1} \,dz \right)dx\\
\end{aligned}
$$
现记 $f(x,z)=e^{-x(1+z)}\cdot x^{p+q-1}\cdot z^{q-1}$ ，
由 $p>1,q>1$ 知 $f(x,z)$ 在 $\mathbb{R}_{\ge 0}^2$ 上非负且连续。

1. 由$$
\begin{aligned}
\Gamma(p+q) 
&= \int_0^{+\infty} e^{-x} x^{p+q-1} dx\\
&= \int_0^{+\infty} e^{-x(z+1)}\cdot [x(z+1)]^{p+q-1} d[x(z+1)]\\
&= \int_0^{+\infty} e^{-x(z+1)}\cdot x^{p+q-1} \cdot (z+1)^{p+q} dx\\
\end{aligned}
$$知：$$
\begin{aligned}
\int_0^{+\infty}f(x,z)dx
&=\int_0^{+\infty}e^{-x(1+z)}\cdot x^{p+q-1}\cdot z^{q-1}dx\\
&=\int_0^{+\infty}e^{-x(1+z)}\cdot x^{p+q-1} \cdot (z+1)^{p+q} \cdot \frac{z^{q-1}}{(z+1)^{p+q}}dx\\
&=\frac{z^{q-1}\cdot \Gamma(p+q)}{(z+1)^{p+q}}
\end{aligned}
$$是 $\mathbb{R}_{\ge0}$ 上关于变量 $z$ 的连续函数；

2. 由$$
\begin{aligned}
\Gamma(q) 
&= \int_0^{+\infty} e^{-z} z^{q-1} dz\\
&= \int_0^{+\infty} e^{-xz}\cdot (xz)^{q-1} d(xz)\\
&= \int_0^{+\infty} e^{-xz}\cdot x^{q} \cdot z^{q-1} dz\\
\end{aligned}
$$知：$$
\begin{aligned}
\int_0^{+\infty}f(x,z)dz
&=\int_0^{+\infty}e^{-x(1+z)}\cdot x^{p+q-1}\cdot z^{q-1}dz\\
&=\int_0^{+\infty}e^{-xz}\cdot x^{q} \cdot z^{q-1}\cdot e^{-x}x^{p-1}dz\\
&=\Gamma(q)\cdot e^{-x}x^{p-1}
\end{aligned}
$$是 $\mathbb{R}_{\ge0}$ 上关于变量 $x$ 的连续函数；

则满足积分号交换的条件，有：
$$
\begin{aligned}
\Gamma(p)\Gamma(q)
&=\int_0^{+\infty} dx  \int_0^{+\infty}  e^{-x(1+z)}\cdot x^{p+q-1}\cdot z^{q-1} \,dz\\
&=\int_0^{+\infty} dz  \int_0^{+\infty}  e^{-x(1+z)}\cdot x^{p+q-1}\cdot z^{q-1} \,dx\\
&=\Gamma(p+q)\cdot \int_0^{+\infty} \frac{z^{q-1}}{(1+z)^{p+q}}\,dz
\end{aligned}
$$
只需证明：
$$
B(p,q)=\int_0^{+\infty} \frac{z^{q-1}}{(1+z)^{p+q}}\,dz
$$
在 $B(p,q)=\int_0^1 x^{p-1}(1-x)^{q-1}\;dx$ 中令 $x=\frac{1}{1+z}$ 即证。

### 分类二
再考虑 $p>0,q>0$ 的情况，有：
利用分部积分法知：
$$
\begin{aligned}
B(p+1,q)
&=\int_0^1 x^{p}(1-x)^{q-1}\;dx\\
&=-\frac{1}{q}\int_0^1 x^{p}\;d(1-x)^{q}\\
&=-\frac{1}{q}\left[0-\int_0^1 (1-x)^{q}\;d\big(x^{p})\right]\\
&=\frac{p}{q}\int_0^1 x^{p-1} \cdot (1-x)^{q}\;dx\\
&=\frac{p}{q}\int_0^1 x^{p-1} \cdot (1-x)\cdot (1-x)^{q-1}\;dx\\
&=\frac{p}{q}\left[\int_0^1 x^{p-1} \cdot (1-x)^{q-1}\;dx-\int_0^1 x^{p} \cdot (1-x)^{q-1}\;dx\right]\\
&=\frac{p}{q}\left[B(p,q)-B(p+1,q)\right]\\
\end{aligned}
$$
即
$$
B(p+1,q)=\frac{p}{p+q}B(p,q)
$$
又因为 $B(p,q)=B(q,p)$ ，故可以得到：
$$
\begin{aligned}
B(p+1,q+1)
&=\frac{p}{p+q+1}B(p,q+1)\\
&=\frac{p}{p+q+1}B(q+1,p)\\
&=\frac{pq}{(p+q+1)(p+q)}B(q,p)\\
&=\frac{pq}{(p+q+1)(p+q)}B(p,q)\\
\end{aligned}
$$
注意到 $p+1,q+1>1$ ，且 $\Gamma(s+1)=s\cdot \Gamma(s)$ ，故利用分类一的结论可以推出：
$$
\begin{aligned}
B(p,q)
&=\frac{(p+q+1)(p+q)}{pq}B(p+1,q+1)\\
&=\frac{(p+q+1)(p+q)}{pq}\cdot \frac{\Gamma(p+1)\Gamma(q+1)}{\Gamma(p+q+2)}\\
&=\frac{\Gamma(p+1)}{p}\cdot \frac{\Gamma(q+1)}{q}\cdot \frac{(p+q+1)(p+q)}{\Gamma(p+q+2)}\\
&=\frac{\Gamma(p)\Gamma(q)}{\Gamma(p+q)}\\
\end{aligned}
$$
证毕。


## 六、常见 $\Gamma$ 函数值

### 1.
$\Gamma(\frac12)=\sqrt{\pi}$ ，
$\Gamma(\frac32)=\frac12\Gamma(\frac12)=\frac12\sqrt{\pi}$ ，
$\Gamma(\frac52)=\frac32\Gamma(\frac32)=\frac34\sqrt{\pi}$ ，
$\Gamma(\frac{2n-1}2)=\frac{(2n-1)!!}{2^n}\sqrt{\pi}$ 。
